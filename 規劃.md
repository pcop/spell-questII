# 拼字王二 —— 二代升級規劃

基準：一代「拼字冒險王」（`../拼字遊戲/`），105 個單字、7 個主題、1638 行單一 `app.js` IIFE。本文件規劃二代的資產沿用、架構升級、新玩法與分期路線圖。

## A. 不變的約束

這些是一代刻意的設計選擇，二代不重新討論，直接繼承：

- **對象**：學齡前～國小低年級，UI 全部正體中文，不能假設孩子看得懂英文說明或錯誤訊息。
- **純前端、無後端、無帳號**——README 開頭就明寫這是設計原則，不是暫時偷懶。
- **觸控優先**：字母排列用點擊 + 觸控拖曳，刻意不用 HTML5 Drag-and-Drop API（觸控裝置支援度差）。
- **行動裝置音訊解鎖**：第一次點擊才能播放音訊/語音（瀏覽器自動播放限制），`unlockAudio()` 模式要保留。
- **`MIN_WORDS_PER_LEVEL`（目前 3）**：單字不足的關卡自動停用的規則保留。

## B. 資產沿用與遷移清單

二代不是從零開始，這是跟「全新專案」最大的差別：

**直接沿用（複製過來，不重做）**
- `data.json`——105 個單字＋`phonics` 拆解規則＋主題定義。內容可以擴充，但既有欄位格式（`id`/`word`/`theme`/`emoji`/`zh`/`syllables`/`phonics`）不變，維持「不改程式碼就能加單字」的核心賣點。
- `words-audio/`、`phonics-audio/`——Edge-TTS 神經語音音檔，重新生成成本高，直接沿用。
- `mascot-images/`——5 角色（柯南／灰原哀／小蘭／安室透／吉莉安）× 9 表情，共 45 張去背 PNG。
- `messages.json`、`tools/generate-neural-audio.py`。

**進度遷移（硬性需求）**
- 讀取一代 `localStorage` 的 `spelling_game_progress_v1`，升版成二代自己的 schema（例如 `schemaVersion: 2`）。**孩子在一代累積的星等/貼紙不能歸零**，這是家長/孩子最容易感受到「被騙」的地方，優先度等同任何新功能。

**一代踩過的坑，重寫時要在程式碼旁註記，避免重踩**：
- three.js `PointsMaterial.sizeAttenuation` 必須設 `false`（正交相機），且尺寸要乘 `renderer.getPixelRatio()`。
- three.js 的 `threefx-ready`/`threefx-error` 事件監聽器要在 `DOMContentLoaded` **之前**同步註冊（`type="module"` 腳本行為等同 defer，比 `DOMContentLoaded` 更早跑完）。
- `ffmpeg` 音訊裁切濾鏡鏈要加 `asetpts=PTS-STARTPTS`，否則後面的 `afade` 用裁切前的絕對時間計算，會把整段音檔淡成靜音（一代曾經有 5 個 chunk 音檔因此完全無聲）。

## C. 架構升級（對應一代已知債務）

| 一代問題 | 二代做法 |
|---|---|
| 1638 行單一 IIFE，靠註解分節導覽 | 拆成 ES modules（資料層／出題邏輯／畫面渲染／音訊／3D 特效各自成檔） |
| **零自動化測試**——README「已知待驗證項目」長達 30+ 條，全靠人工開瀏覽器確認 | 至少補：(1) 資料一致性測試（`phonics.chunks`/`syllables` 串接＝`word`、音檔對應完整）(2) 判分/出題邏輯的單元測試，這兩類不需要瀏覽器就能跑，CI 可擋 |
| three.js 是 CDN 硬依賴，連不上就整個拼字關卡鎖死 | 改本地 vendor（不依賴外部 CDN 可用性），並評估降級成「可選」而非硬性門檻（見 F 決策） |
| phonics chunk 用「拼法文字」查音檔，同拼法不同發音只能取一個代表音（`oo`/`ear` 不準，`fly` 靠 `audioOverrides` 補丁） | ~~改成 per-word chunk 音檔~~ **改為沿用既有 `audioOverrides` 機制修剩下的 `oo`/`ear`**（2026-09-06 執行時修正：per-word 重寫需要對 97 個字逐一做語音邊界判定，產出的音檔裁切點沒辦法用耳朵驗證，投入產出比太差；`audioOverrides` 已經是驗證過可行的機制，`fly` 就是先例，多加 3 筆設定即可解掉 `book`/`foot`/`pear`/`heart` 這 4 個字，零程式碼改動） |
| 難度規則「長度分級 xor 自訂關卡」二選一，同主題不能混用 | 統一成一套 level 定義格式，兩種來源都能共存在同一主題 |

## D. 新玩法（依「投入產出比」排序，不是功能大雜燴）

1. **錯題本／複習模式／間隔重複**——最高優先，因為零新資料成本：一代的 `progress.levels[].wordProgress` 已經在記錄每個單字的 correct/wrong 次數，但只拿來算星等，從沒被用來決定「這個字要不要多考」。二代把這份既有資料接上一個複習出題邏輯即可，是最省力但最有感的功能。✅ 已完成，見下方 Phase 3。
2. **吉祥物表情包接上情緒**——~~一代 `setMascotCharacter()` 永遠只載入 `<id>.png`（中性表情）~~（2026-09-06 執行時修正：這條判斷錯了，一代其實本來就會用 `cfg.prefix + '_' + expr` 隨機輪播全部 45 種表情，只是**隨機**而非**依事件**挑選）。二代真正的升級點是把隨機挑表情改成依事件挑表情（答對→開心系表情、答錯→委屈系表情、按提示→思考表情），這樣才是真的「有感」而不是原地打轉。✅ 已完成，見下方 Phase 3。
3. **多小孩 profile**——同一裝置多個孩子輪流玩，各自的進度不互相覆蓋（仍是 `localStorage`，不需要帳號系統）。⏸ 本輪刻意不做，見下方「後續 backlog」。
4. **新出題模式**：純聽寫（不給圖片提示的加強版）、圖字配對、限時挑戰、每日任務。句子層級的練習留到更後期，不在二代範圍內。⏸ 本輪刻意不做，見下方「後續 backlog」。
5. **內容擴充**：新主題、動詞/形容詞、sight words（常見字），沿用 B 段的 `data.json` 格式即可加入，不算架構升級。⏸ 本輪刻意不做，見下方「後續 backlog」。

## E. 分期路線圖（全部完成，2026-09-06）

**✅ Phase 0：骨架與資產遷移**
Vite 專案骨架、105 字 `data.json`／音檔／吉祥物圖／`messages.json` 原封不動搬遷，進度 schema v1→v2 遷移＋匯入防呆，資料一致性自動化測試，three.js 改本地 npm 依賴。

**✅ Phase 1：功能對等（不能是「升級版卻退步」）**
一代 README「功能」章節列的每一項都已在二代重新實作：game 引擎（出題/判分/關卡定義/提示安全閥/逐題持久化）、audio 引擎（神經語音/音效/phonics 查找）、three-fx（粒子特效/獎盃禮物盒寶箱模型/開寶箱動畫）、完整 UI（主選單→選主題→選難度→遊戲→結果、字卡瀏覽、拼讀練習、我的進度頁含匯出入/貼紙簿/3D 診斷）。
驗收：73 個自動化測試（含一支 jsdom 整合 smoke test 模擬「開始遊戲→整關全對→結果畫面」等 4 條主線路徑，彌補沒有瀏覽器擴充套件可用時的驗證缺口）全部通過；後續 Phase 1.5 用 Playwright + 真實 Chrome 實機截圖，額外驗證出一個一代就存在、這次才發現的 CSS 特異度 bug（見下方）。

**✅ Phase 1.5：視覺風格重新設計（使用者追加需求，2026-09-06）**
主色調從一代蒼白粉彩改為「夜空冒險」深靛紫漸層＋珊瑚橙/青綠行動色，漫畫感厚邊框＋立體投影的按鈕/面板語言，字體改用 Baloo 2＋Nunito，設計 token 全部定義在 `:root` 供之後沿用。修正多處低對比文字（我的進度頁統計文字、選難度頁鎖定關卡說明）。過時文案「跟著貓頭鷹博士」改掉（吉祥物早已是柯南角色群不是貓頭鷹）。**意外抓到一個一代就存在的真實 bug**：`#btn-next-question { display:block }` 用 ID 選擇器的 CSS 優先權蓋過了 JS 用 `hidden` 屬性做的顯示控制，導致「下一題」按鈕在答對前就會顯示，這次一併修正。
驗收：Playwright + 真實 Chrome 走完全部主線畫面並逐張截圖比對，確認文字清晰、按鈕好點、動畫正常、73 個自動化測試全過。

**✅ Phase 2：架構升級**
模組化拆分（game/audio/ui/three-fx/progress/data 各自成 ES module）、73 個自動化測試、three.js 本地化、`getLevelDefsForTheme` 統一吃 `difficultyTiers`/`customLevels`/`review` 三種來源。Phonics `oo`/`ear` 不準的問題改用既有 `audioOverrides` 機制解決（見上方 C 段修正說明），零程式碼改動。
驗收：`npm test` 一鍵跑完不需要瀏覽器；`vite.config.js` 加 `manualChunks` 消除 build 警告。

**✅ Phase 3：新玩法（優先兩項，範圍見下方「後續 backlog」）**
D 段第 1、2 項已完成：**錯題複習模式**（每個主題底下的虛擬關卡 `kind:'review'`，沿用既有拼字關卡畫面/判分/持久化，零新 UI，答對複習不重複發貼紙）、**吉祥物表情依事件挑選**（答對/答錯/提示/過關/待機各自對應合理的表情子集，不再是純隨機）。
驗收：73 個自動化測試（含新增的 8 個錯題複習測試）全過，Phase 1.5 的截圖驗證也涵蓋了改版後的畫面。

## F. 決策（已拍板）

1. **重構方式：逐模組重構**——從一代搬遷過來、拆模組重構，不全新重寫。B 段列的那些坑（three.js 相機模式、音訊淡出時間戳、chunk 靜音偵測）直接帶著走，不重踩。
2. **建置工具：引入 Vite**——二代不再是零工具的雙檔案專案，改用 `npm install`/`npm run dev`/`npm run build`。ES modules 拆分＋自動化測試（C 段）直接建立在 Vite 專案結構上，不用另外土法煉鋼配 module 載入。
3. **three.js：維持硬性需求，不做 2D 備援**——沿用一代行為，CDN/WebGL 失敗時拼字關卡鎖死並提示原因；但**改本地 vendor（不依賴外部 CDN）**這點（C 段已列）仍然要做，降低「連不上 CDN」這個失敗模式本身發生的機率，跟「要不要備援」是兩件事。

---

## G. 後續 backlog（本輪刻意不做，非遺漏）

以下項目在規劃階段就明列在 D 段，但這輪只做了投入產出比最高的前兩項（錯題複習、mascot 表情），其餘刻意不展開，避免變成「功能大雜燴」：

- **多小孩 profile**：同一裝置多個孩子輪流玩，各自進度不互相覆蓋。需要新增一層「目前使用者」狀態＋切換 UI，範圍不小，值得單獨立項規劃。
- **新出題模式**：純聽寫（無圖片提示的加強版）、圖字配對、限時挑戰、每日任務。每一種都是一個新的遊戲模式，需要各自的畫面設計，不是「加一個函式」規模的工作。
- **內容擴充**：新主題、動詞/形容詞、sight words。`data.json` 格式已經支援，隨時可以加，純粹是內容產出（含對應音檔生成）的工作量問題，不是技術限制。
- **句子層級練習**：規劃階段就標記「不在二代範圍內」，維持不變。

這些項目不需要架構變動就能個別動工，之後想推進任何一項，直接在這份文件底下開新的 Phase 即可。

---

**二代升級完成狀態（2026-09-06）**：Phase 0～3 全部完成，73 個自動化測試通過，`npm run build` 正常，Playwright + 真實 Chrome 實機驗證過視覺與主線互動流程。開發方式：`npm install && npm run dev`。
